import { Routes } from '@angular/router';
import { TimestableComponent } from './components/timestable/timestable.component';

export const routes: Routes = [{
    path: '',
    component: TimestableComponent
}];
